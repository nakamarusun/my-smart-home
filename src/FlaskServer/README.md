# SmartHome web server for Raspberry PI

## Instructions:

1. Install MySQL Database di RPI
```shell
sudo apt update
sudo apt install mariadb-server
```

2. Setel password dan akses rootnya
```shell
sudo mysql_secure_installation
```